import { LowerCasaPipe } from './lower-casa.pipe';

describe('LowerCasaPipe', () => {
  it('create an instance', () => {
    const pipe = new LowerCasaPipe();
    expect(pipe).toBeTruthy();
  });
});
